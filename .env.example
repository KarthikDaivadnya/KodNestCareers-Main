# ─────────────────────────────────────────────────────────────
# KodNestCareers — Shared Environment Variables Template
# Copy this file to .env.local (NEVER commit actual .env files)
# ─────────────────────────────────────────────────────────────

# ── App environment ──────────────────────────────────────────
NODE_ENV=development
VITE_APP_ENV=development

# ── API base (set per-app in Vercel project env) ─────────────
VITE_API_URL=https://api.kodnestcareers.com

# ── AI Resume Builder ─────────────────────────────────────────
VITE_OPENAI_API_KEY=sk-...
VITE_OPENAI_MODEL=gpt-4o

# ── Job Tracker ───────────────────────────────────────────────
# (no external keys needed — localStorage-only MVP)

# ── Placement Platform ────────────────────────────────────────
# VITE_SUPABASE_URL=https://xxxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJ...

# ── Future: Analytics ─────────────────────────────────────────
# VITE_POSTHOG_KEY=phc_...
# VITE_POSTHOG_HOST=https://app.posthog.com
